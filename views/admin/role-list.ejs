<!DOCTYPE html>
<html lang="en">
<head>
 <%- include('../partials/head') %>
</head>
<body>
    
    <%- include('../partials/nav') %> 
        <div class="container mt-3 ">
            
            <div class="col-md-12 mt-3" >
                <!-- bize action=edit bilgisinden bir değer geliyorsa ve bu gelen değer edit ise bize üstden bid div blog güncellendi diye bir alert atacaktır. -->
                
                <table class="table table-bordered table-striped" >
                    <thead>
                        <tr>
                            <th style="width:50px;">id</th>
                            <th style="width:150px;">role ismi</th>
                            <th>kullanıcı sayısı</th>
                            <th style="width:130px;">id</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% roles.forEach(role => { %>
                            <tr>
                                <td># <%= role.id %></td>
                                <td>
                                    <%= role.rolename %>
                                </td>

                                <td>
                                   <p><%= role.user_count %></p> 
                                </td>   
      
                                <td>
                                    <a class="btn btn-sm btn-primary" href="/admin/roles/<%= role.id %>">düzenle</a>
                                    <a class="btn btn-sm btn-danger" href="/admin/role/delete/<%= role.id %>">sil</a>
                                </td>
                            </tr>
                        <% }) %>

                    </tbody>
                </table>
            </div>
        </div>
</div>

</body>
</html>


